package pmesp.helpdesk37bpmm.Chamado.enums;

// Informa quem resolveu o chamado sem multiplicar estados de status.
// O status responde "em que etapa está"; este enum responde "quem resolveu".
public enum ChamadoResolvidoPor {
    MIKE_IA,
    TECNICO
}
